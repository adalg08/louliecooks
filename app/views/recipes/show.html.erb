<div class="container text-center padded">

  <h2><%= @recipe.name %></h2>

  <% if @recipe.photo? %>
   <p><%= cl_image_tag @recipe.photo.path, width: 300, height: 300, crop: :fill %></p>
  <% end %>

  <div class="ingredients">
    <h3>Ingredients</h3>
    <ul>
      <% @recipe.compositions.each do |composition| %>
        <li><%= composition.ingredient.name.capitalize %> <%= link_to "🚮", composition_path(composition), method: :delete,  data: { confirm: "T'es sûr-e de voulouir eliminer cet ingredient ?" }  %></li>
      <% end %>
    </ul>
    <%= simple_form_for [@recipe, @composition] do |f| %>
      <%= f.input :ingredient, label: false %>
      <%= f.button :submit, 'Ajouter ingredient' %>
    <% end %>
  </div>

  <div class="recipe-description">
    <%= @recipe.description %>
  </div>

  <div class="categories">
    <h3>Parfaite pour quelle ocassion ?</h3>
    <ul>
      <% @recipe.groups.each do |group| %>
        <li><%= group.category.name.capitalize %> <%= link_to "🚮", group_path(group), method: :delete,  data: { confirm: "T'es sûr-e de voulouir eliminer cet ocassion ?" }  %></li>
      <% end %>
    </ul>

    <%= simple_form_for [@recipe, @group] do |f| %>
      <%= f.association :category, label: false %>
      <%= f.button :submit, 'Ajouter ocassions' %>
    <% end %>

  </div>

</div>

